// GraphQL is not super different than REST
// you make an HTTP request (GET, POST) with some params and it responds with the requested data

// Schema
// defines the set of data that the client can access (objects, fields)
